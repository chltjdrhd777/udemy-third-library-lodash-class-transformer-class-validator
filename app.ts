import _ from "lodash"; // calling lodash

console.log(_.shuffle([1, 2, 3])); // "_.shuffle()" is lodash syntax that allows me to make a randome array by using elements inside the array like [2,3,1] or [3,1,2]
